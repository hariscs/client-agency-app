import styles from '../../styles/components/about/Vision.module.css';

const Vision = () => {
	return (
		<section className={styles.vision}>
			<div className='container'>vision and mission</div>
		</section>
	);
};

export default Vision;
